# start_date = '2019-12-01'
    # end_date = '2019-12-18'
    # campaigns = get_campaigns()
    # received_data = get_metric_data_from_export_for_campaigns("Received Email", campaigns, start_date, end_date)
    # with open('received_email_data.json', 'w') as outfile:
    #     json.dump(received_data, outfile)
    # opened_data = get_metric_data_from_export_for_campaigns("Opened Email", campaigns, start_date, end_date)
    # with open('opened_email_data.json', 'w') as outfile:
    #     json.dump(opened_data, outfile)
    # bounced_data = get_metric_data_from_export_for_campaigns("Bounced Email", campaigns, start_date, end_date)
    # with open('bounced_email_data.json', 'w') as outfile:
    #     json.dump(bounced_data, outfile)